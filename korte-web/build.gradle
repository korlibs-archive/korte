apply plugin: 'kotlin'

dependencies {
    compile project(":korte")
    compile "com.soywiz:korio-ext-web:$korVersion"
}

configurations {
    provided
}

repositories {
    mavenLocal()
    jcenter()
    mavenCentral()
}

kotlin {
    experimental { coroutines 'enable' }
}
